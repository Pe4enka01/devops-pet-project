# 1. Базовый образ (максимально легкий)
FROM python:3.11-slim

# 2. Установка рабочей директории внутри контейнера
WORKDIR /code

# 3. Копируем только файл зависимостей (для кэширования слоев)
COPY requirements.txt .

# 4. Установка библиотек
# --no-cache-dir уменьшает размер итогового образа
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем остальной код (main.py) в текущую папку (/code)
COPY . .

# 6. Пробрасываем порт (чисто информативно для Docker)
EXPOSE 8000

# 7. Команда запуска
# host 0.0.0.0 обязателен, чтобы сервер "слушал" запросы извне контейнера
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]